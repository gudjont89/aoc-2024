pub fn run_first(is_real: bool) -> usize {
    0
}

pub fn run_second(is_real: bool) -> usize {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    // #[test]
    // fn test_run_first() {
    //     assert_eq!(run_first(false), xx);
    // }

    // #[test]
    // fn real_run_first() {
    //     assert_eq!(run_first(true), xx);
    // }

    // #[test]
    // fn test_run_second() {
    //     assert_eq!(run_second(false), xx);
    // }

    // #[test]
    // fn real_run_second() {
    //     assert_eq!(run_second(true), xx);
    // }
}
